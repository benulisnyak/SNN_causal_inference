# Ground-truth networks

The included YAML file is an example directed ground-truth network used by the project. Each node stores its position, outgoing targets, and signed outgoing weights. Positive weights denote excitatory source neurons and negative weights denote inhibitory source neurons.

The example is useful for inspecting the expected schema and testing network-loading utilities. Full model evaluation requires the ground-truth YAML corresponding to each evaluated network instance.
